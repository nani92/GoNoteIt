mutation GetChangelog($timestamp: Int!) {
    changelog(timestamp: $timestamp) {
       created {
        uuid,
        id,
        type,
        data,
        updatedAt
       },
       updated {
        uuid,
        id,
        type,
        data,
        updatedAt
       },
       deleted
     }
}
